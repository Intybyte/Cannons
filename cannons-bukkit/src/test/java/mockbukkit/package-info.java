/**
 * this module has mostly stuff taken from mockbukkit, as registry access and stuff
 * can't be mocked if not using java 21 (which would break compatibility with older mc versions)
 * I am reimplementing it here myself
 */
package mockbukkit;